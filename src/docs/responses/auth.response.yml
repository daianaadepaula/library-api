Signup:
  description: Register a new user
  content:
    application/json:
      schema:
        $ref: '../schemas/auth.schema.yml#/components/schemas'

Login:
  description: Login user
  content:
    application/json:
      schema:
        $ref: '../schemas/auth.schema.yml#/components/schemas'

Logout:
  description: Logout user
  content:
    application/json:
      schema:
        type: object
        properties:
          message:
            type: string
            example: User logged out successfully
