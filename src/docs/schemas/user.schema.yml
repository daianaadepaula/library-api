type: object
properties:
  name:
    type: string
    format: name
  email:
    type: string
    format: email
  password:
    type: string
    format: password
required:
  - name
  - email
  - password
